/**
 * constants.ts
 * ---------------------------------------------------------------------------
 * Small app-wide constants that aren't per-project data. Currently just the
 * agency's own name, shown to clients on the public calendar so the "RJF"
 * internal lane reads as a real, branded name rather than jargon.
 */
export const AGENCY_NAME = "RunJumpFly";
